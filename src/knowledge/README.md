# Private assistant corpus (public at build time)

Add `.txt` or `.md` notes here that the on-site smart search should understand.
Do not add passwords, API keys, private contact details, or unpublished secrets:
these files are bundled into the public static site at build time.
